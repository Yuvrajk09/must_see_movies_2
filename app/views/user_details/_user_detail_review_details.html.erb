<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Review Details

    <a data-toggle="collapse" href="#user_detail_review_details_new_form">
      Add review detail
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @user_detail.review_details.each do |review_detail| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to review_detail, review_detail %>

        <%= link_to review_detail, method: :delete, data: { confirm: "Do you really want to delete this review_detail" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="user_detail_review_details_new_form">
        <%= render partial: "user_details/user_detail_review_details_new_form" %>
      </div>
    </div>

  </ul>
</div>
